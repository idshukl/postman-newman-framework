{
    "name": "postman-newman-framework",
    "version": "1.0.0",
    "description": "Framework to execute Postman collections with Newman",
    "scripts": {
      "test": "newman run collections/sample-collection.json -e environments/sample-environment.json --reporters cli,htmlextra --reporter-htmlextra-export reports/test-report.html"
    },
    "dependencies": {
      "newman": "^6.0.0",
      "newman-reporter-htmlextra": "^1.23.1"
    }
  }
  
